#include "main.h"
/**
*positive_or_negative - kjge
*
*@n: eghe
*Return: njhtr
*/
void positive_or_negative(int n)
{
	if (n < 0)
		printf("%d is negative", n);
	else if (n == 0)
		printf("%d is zero", n);
	else
		printf("%d is negative", n);
}
